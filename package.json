{
  "name": "node-chat-app",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "node server/src/server.js",
    "heroku-postbuild": "cd client && yarn && yarn run build"
  },
  "dependencies": {
    "express": "^4.16.4"
  },
  "engines": {
    "node": "8.12.0"
  }
}